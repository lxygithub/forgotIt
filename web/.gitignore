# dependencies
node_modules/

# next.js
.next/
out/
build/

# env
.env
.env*.local

# Z.ai AI credentials (never commit, see forgotIt部署文档.md §5)
.z-ai-config

# database (local sqlite, generated by `bun run db:push`)
db/

# runtime user uploads
public/uploads/

# logs & misc
*.log
.DS_Store
.vercel
*.tsbuildinfo

# cloudflare workers (双部署，v1.3)
.wrangler/
.open-next/
.dev.vars

# Workers 专用 Prisma 客户端（bun run build:cf 自动生成）
src/generated/

# Cloudflare 部署凭证（gitignored，勿入库）
.cf-*
